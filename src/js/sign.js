// const header = new URL(document.location).searchParams,
//       name = document.

// if (header.get("sign") !== "404") {
//   info.innerHTML += `${header.get("info")}`;
// } else if (header.get("info") === "404") {
//   info.innerHTML = "Ошибка 404! Данной страницы не существует";
// }